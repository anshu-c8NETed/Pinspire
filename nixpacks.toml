[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
```

### Option 2: Use Railway's automatic detection
Create a `.railwayignore` file (if you have unnecessary files):
```
node_modules/
.git/
*.log
.env.local
